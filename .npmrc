# pnpm + Metro (React Native) compatibility
# Metro bundler requires hoisted node_modules
node-linker=hoisted
shamefully-hoist=true

# Allow peer dependency mismatches (common in RN/Expo ecosystem)
strict-peer-dependencies=false
